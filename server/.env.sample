# Environment Variables Template for Your Project
# Replace placeholder values with actual secrets during setup

# Application Port
PORT=your-port-number

# MongoDB Database URL
DATABASE_URL="your-mongodb-connection-url"

# NextAuth Secret (used for encrypting tokens)
AUTH_SECRET=your-auth-secret
